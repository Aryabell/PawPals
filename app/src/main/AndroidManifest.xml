<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PawPals">

        <meta-data
            android:name="android.support.vectordrawable"
            android:value="true"/>

        <!-- Activity utama yang jadi launcher -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity untuk membuat post baru -->
        <activity
            android:name=".NewPostActivity"
            android:exported="false" />

        <!-- Activity untuk melihat & membalas post -->
        <activity
            android:name=".ReplyActivity"
            android:exported="false" />

    </application>

</manifest>
